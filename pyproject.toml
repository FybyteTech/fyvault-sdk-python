[project]
name = "fyvault"
version = "0.6.0"
description = "FyVault SDK for Python"
requires-python = ">=3.9"
dependencies = [
    "httpx>=0.27",
    "typing_extensions>=4.0",
]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
